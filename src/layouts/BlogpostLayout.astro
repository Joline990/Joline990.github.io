---
const { description } = Astro.props;
---

<style>
    .blog_content__description {
        font-style: italic;
        font-weight: medium;
        border-left: var(--fluid-border-width) solid var(--color-text);
        padding-left: 1rem;
        margin-bottom: 1rem;

        @media screen and (min-width: 1100px) {
            font-size: 1.5rem;
        }
    }
    .blog_content:not(:has(.sidebar.sticky-active)){
            max-width: 75ch;
            margin: auto;
    }
</style>
    <div class="blog_content">
        {description?.trim() &&(<p class="blog_content__description">{description}</p>)}
        <slot />
    </div>
